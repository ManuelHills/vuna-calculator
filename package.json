{
  "name": "vuna-calculator",
  "version": "1.0.0",
  "description": "VUNA Calculator — CI/CD Pipeline Assignment (Set 2025/26)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --coverage --forceExit",
    "test:ci": "jest --coverage --ci --forceExit --coverageReporters=text"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
